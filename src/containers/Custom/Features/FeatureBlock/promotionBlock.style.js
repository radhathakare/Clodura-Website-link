import styled from 'styled-components';

const BlockWrapper = styled.div`
  max-width: 70%;
  padding: 0;
  margin: 80px auto;
  text-align: center;

  p {
    font-size: 16px;
  }
`;

export default BlockWrapper;

import styled from "styled-components";

const SubbannerWrapper = styled.section`
    margin: 0 auto;
    max-width: 70%;
    padding: 0;
    display: flex;
    flex-direction: column;
    text-align: center;

    button {
        flex: 0.24;
    }

    p {
        font-size: 16px;
    }
`;

export default SubbannerWrapper;